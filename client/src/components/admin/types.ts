// Import types from shared schemas to ensure consistency
import type { AudioAd, BannerAd } from "../../../../shared/schemas";

// Re-export for backward compatibility
export type { AudioAd, BannerAd };
